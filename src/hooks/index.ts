/**
 * Custom Hooks
 *
 * Reusable React hooks for common functionality.
 * Follow the "use" prefix convention.
 *
 * Export all custom hooks here:
 * import { useMediaQuery, useScroll } from '@/hooks';
 */

export {
  useMediaQuery,
  useIsMobile,
  useIsTablet,
  useIsDesktop,
} from "./useMediaQuery";
export { useScrollPosition, useIsScrolled } from "./useScrollPosition";
